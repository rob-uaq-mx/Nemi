# prueba_afirma — fixture de prueba: una `afirma` deliberadamente falsa,
# para comprobar que el error de "afirma" (v0.2 §21.2) señala ESTE archivo
# cuando se carga vía `incluye`, no el que lo incluye (spec §12).
afirma 1 = 2, "esto debe fallar en prueba_afirma.nemi"
